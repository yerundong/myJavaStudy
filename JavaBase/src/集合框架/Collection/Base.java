package 集合框架.Collection;

import org.junit.jupiter.api.Test;

/**
 @Collection接口: 统一定义了一套单列集合的接口，它规定它的子接口及其实现类都必须满足它重新设定的规则。是List和Set的父接口。
 */
public class Base {
    /**
     * @Description 向集合中添加一个元素。集合更改则添加成功返回true，如果该集合不允许重复并且已经包含指定的元素。返回false。部分子类的add方法可能会限制添加到集合中的元素类型，或者不会将NULL添加到集合中。
     */
    @Test
    public void add(){

    }
}
