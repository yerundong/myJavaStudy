package JDBC.DAO;

public class BaseDAO<T> {

    // public boolean update(Connection cnn, String sql, Object... args) {
    //     PreparedStatement ps = cnn.prepareStatement(sql);
    //
    // }
}
