<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace：命名空间标签，必须写 -->
<mapper namespace="userMapper">
    <select id="selectAll" resultType="pojo.User">
    select * from user
  </select>
</mapper>